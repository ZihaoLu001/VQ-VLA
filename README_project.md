# UniTok + VQ-VLA Integration (WIP)

This branch integrates UniTok visual tokens into the VQ-VLA / OpenVLA training pipeline.

Milestones:
1) Reproduce VQ-VLA baseline.
2) Load GATE-VLAP dataset and extract (image, instruction, actions).
3) Precompute UniTok visual tokens offline.
4) Train VLA with UniTok visual tokens + VQ-VLA action tokens.
